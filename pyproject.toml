[project]
name = "memi-engine"
version = "0.2.0"
description = "Engine for building memi memory card game instances"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Filipa Andrade", email = "filipa.andrade@gmail.com" }]
readme = "README.md"
requires-python = ">=3.10"
keywords = ["memi", "memory", "game", "education", "flask", "quiz"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Education",
    "License :: OSI Approved :: MIT License",
    "Framework :: Flask",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Games/Entertainment",
    "Topic :: Education",
]
dependencies = [
    "flask>=3.0",
    "requests>=2.31",
]

[project.optional-dependencies]
server = ["gunicorn>=22"]
dev = ["pytest>=8", "ruff>=0.6"]

[project.urls]
Homepage = "https://memi.click"
Repository = "https://github.com/filias/memi-engine"
Issues = "https://github.com/filias/memi-engine/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["memi_engine"]

[tool.hatch.build]
include = [
    "memi_engine/**/*.py",
    "memi_engine/py.typed",
    "memi_engine/templates/**",
    "memi_engine/static/**",
]

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
